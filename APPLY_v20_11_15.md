# Apply v20.11.15

From the repository root:

```bash
git pull origin main && unzip -o kibun-v20.11.15.zip -d . && bash apply_update.sh
```

The update script commits and pushes the changed SNS Audit files.
