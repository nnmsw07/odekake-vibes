# Kibun Plans 設計 v19.8

Kibunの主語を「場所」から「一日の過ごし方」へ移す。

## 3種類
- **ONE PLACE**: 1〜2時間、半日、1日を単一施設で過ごす。
- **DAY PLAN**: 短めの主役スポット＋同じエリアの寄り道を2ストップで構成。
- **STAY PLAN**: 日中の行き先＋ホテル、またはホテルを中心とした1泊案。

## 単一スポット判定
利用可能時間が短い場合、または推定滞在時間が利用可能時間の約6割以上を占める場合は、無理に寄り道を追加しない。特に半日で完結するテーマパーク・大型ミュージアム・複合施設などを尊重する。

## 2ストップ判定
主役スポットだけでは時間が余る場合に限り、同一ローカルエリアで以下を考慮して補助スポットを選ぶ。
- 同一 recommendation_group を避ける
- 営業・年齢・宿泊モード等の既存ハードフィルターを維持
- 主役と異なるカテゴリを優先
- 文化/体験/自然 → 食、アクティブ → 温浴など補完関係を評価
- 予定時間を大きく超えない

## UX
トップは気分を選ぶ体験を維持し、結果だけを「場所」から「過ごし方」へ昇格させる。地域×カテゴリ画面は引き続きスポット検索の役割を持つ。

## v19.8.1 refinement
- Planカード上部の時間は、スポット単体の滞在時間ではなくユーザーが選んだ「使える時間」を表す。
- 各スポットの実際の滞在目安はFLOW内で別に表示する。
- 半日以上の指定では、Plan全体が選択時間の約6割を満たすことを最低ラインとする。
- 短い主役＋近隣スポットでも時間が埋まらない場合、短い単独Planへ落とさず別の主役候補へバックフィルする。
- 汎用Planコピーは抽象的な比喩を避け、個別編集コピーがない場合ほど自然で具体的にする。

## v19.8.5 editorial blueprint layer

自動組み合わせだけでは「近いが意味のないハシゴ」が残るため、代表エリアには編集済み `CURATED_PLANS` を重ねる。主役スポットが一致し、同行者・選択vibe・利用可能時間が合う場合のみ使用し、合わなければ従来の動的Plan生成へfallbackする。

子連れ長時間Planの基本形は `大人も楽しむ → 子どもの時間 → ひと休み・ごはん`。ただし単一施設で時間が成立する場合はONE PLACEを維持する。shopping hard intentは主役選定にのみ強く適用し、companionは遊び・食事など補完役を許可する。
