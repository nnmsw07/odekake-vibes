# Apply v20.11.22

From the repository root:

```bash
git pull origin main && unzip -o kibun-v20.11.22.zip -d . && bash apply_update.sh
```

Cloudflare deployment is handled by the existing GitHub integration after push.
