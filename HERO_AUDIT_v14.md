# Kibun Hero Audit v0.14

Google Places連携後に「施設は合っているがHeroとして弱い写真」が混ざる問題を、実写真を見ながら選べるようにする管理用機能です。

## 開き方

通常URLには監査UIは出ません。

```
https://nnmsw07.github.io/odekake-vibes/?heroAudit=1
```

で開くと、画面下部に **Hero監査** ドックが出ます。

1. スポットを選ぶ
2. `開く`
3. 詳細画面下部の `Hero写真を選ぶ` で候補#0〜#5を比較
4. 一番「この場所に行きたい」が伝わる写真をタップ
5. 選択はこの端末のlocalStorageだけに保存
6. 数件選び終えたら `設定を書き出す`

書き出されるJSON例:

```json
{
  "spot_015": 2,
  "spot_023": 1
}
```

このJSONの index を各スポットの

`media_strategy.google_places.photo_index_override`

へ反映すると、全ユーザーに固定できます。

## Hero選定基準

優先順位は以下です。

1. 実際の施設・体験内容が一目でわかる
2. 人物が主役すぎず、施設の空気感が伝わる
3. スマホの横長トリミングでも主役が残る
4. 暗すぎない・文字だらけでない
5. 外観だけより、可能なら「そこで何ができるか」が伝わる
6. 子連れ専用感に寄りすぎず、Kibunの大人向けUIにも馴染む

## APIコスト

監査モードで施設を開いたときだけ `/place-photos` を呼びます。通常利用では呼びません。
1回のPlace検索で候補写真をまとめて取得するため、候補ごとにText Searchを繰り返さない設計です。
