# CHANGELOG v19.8.2

## Plan quality

- 「半日くらい」「たっぷり半日」では、`泊まりもあり` が選ばれていても宿泊Planを生成しないよう修正。
- 宿泊Planは `使える時間 = 1日` かつ `泊まりもあり` のときだけ候補化。
- 横浜市 / 川崎市は市全体ではなく区単位で companion を探索。東京23区も区単位。
- 公園→公園、美術館→美術館など同系統ハシゴのスコアを大幅に下げ、自動Planでは原則回避。
- 子連れPlan向けに `childFocusScore` / `adultFocusScore` / `familyBalanceScore` を追加。
- たっぷり半日〜1日の子連れPlanでは、主役と異なる役割のスポットを優先し、余裕があれば食事・カフェ系を3つ目に追加。
- 食事/休憩用途の判定を `isMealSpot` として分離し、food museum 等をカフェ代わりに扱わないよう改善。
- 1日Planなのにタイトル本文が「半日」となるコピー不整合を修正。
- Plan間の移動バッファを2スポット以降それぞれ30分として計算。

## UI

- `今日帰る？` の注記を更新し、短時間・半日は日帰り提案になることを明記。
- `plans.js` / `app.js` のcache busterを v1982 に更新。

## Tests

- `test_v19_8_2_plan_quality.js` を追加。
- 半日へのホテル漏れ、区単位近接、公園ハシゴ抑制、親子役割＋食事の3-stop構成を回帰テスト化。
