# Apply v20.11.14

From the repository root:

```bash
git pull origin main && unzip -o kibun-v20.11.14.zip -d . && bash apply_update.sh
```

The update has already been syntax/regression tested before packaging.
