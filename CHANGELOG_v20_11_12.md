# v20.11.12

- Replaced the single repeated photo-less `KIBUN NOTE` card with five rotating editorial layouts.
- Card order is deterministic within each carousel: quote → mood check → best for → Kibun moment → quick note.
- Each layout uses different typography, information hierarchy, and decorative treatment so consecutive no-photo slides no longer look identical.
- Existing reusable-photo behavior is unchanged: if a strict safe image resolves, the note card upgrades to the photo.
- Safe-mode AI cover behavior remains unchanged.
