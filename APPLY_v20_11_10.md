# Apply v20.11.10

Upload `kibun-v20.11.10.zip` to the repository root, then run:

```bash
git pull origin main && unzip -o kibun-v20.11.10.zip -d . && bash apply_update.sh
```

The supplied apply script commits and pushes the update.
