# v20.8.3 — Mobile image overlay fix

- Added the missing `.image-shell` positioning/overflow container rule.
- Prevents image gradient pseudo-elements and `イメージ` badges from escaping their cards on mobile browsers.
- Bumped the stylesheet cache-buster to `v=2083`.
- No spot, affiliate, hero override, Worker, or image-asset changes.
