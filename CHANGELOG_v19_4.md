# CHANGELOG v19.4

## 2026-08-29

### Hero監査を反映
- ユーザーの最新Hero監査exportを `HERO_OVERRIDES_v19_4.json` として保存。
- `photo_index_override`: **153スポット**へ反映。
- `place_overrides`: **28件**を反映。
- seed全体ではGoogle Place ID設定済みスポットが **37件**。
- `seed.json` / `data.js` に反映済み。

### 出発地を自由に選択可能に
従来の「現在地から」だけではなく、条件欄を **「どこから出かける？」** に変更。

- 現在地を使う
- 駅名を検索（例: 横浜駅）
- 地名を検索（例: みなとみらい）
- 住所を検索
- 出発地を指定せず、距離条件なしでも利用可能

場所検索はCloudflare Worker経由のGoogle Places Text Searchを使用。
新endpoint:

- `GET /location-search?q=横浜駅`

返却された候補からユーザーが出発地を選択し、その緯度経度から既存の `/travel-times` でルート時間を計算する。

### Kibunロゴをブラウズ入口に
左上の `Kibun` ロゴを押すと、全211スポットを自由に眺められるブラウズ画面を開くように変更。

**地域から**
- 神奈川
- 横浜
- 川崎
- 湘南・鎌倉
- 三浦・横須賀
- 箱根・県西
- 東京
- 千葉
- 埼玉

**カテゴリから**
- 子ども・遊び
- 公園・自然
- 水遊び・プール
- 動物・水族館
- アート・文化・本
- カフェ・グルメ
- 買い物・複合施設
- 温浴・リラックス
- 体験・学び

スポット名・地域のフリーワード検索も可能。

### Worker
**v19.4ではWorker更新あり。**
`/location-search` を追加しているため、フロントだけでなく `worker/worker.js` もCloudflareへ再デプロイが必要。
