# Hero Rescue v16

`?heroAudit=1` でHero監査を開きます。

写真が取れない施設では、原因とGoogle側の候補名を表示します。検索名をGoogle Maps上の名称に近づけて「再検索」してください。必要なら「住所も検索に使う」をOFFにします。

一致度がLOWの候補も監査モードだけは写真を確認できます。正しい施設だと目視確認できた場合のみ「このGoogle施設を固定」を押してください。Place IDが端末内に保存され、その後の監査では名前検索に依存しません。

最後に「設定を書き出す」を押すと、写真番号とPlace ID固定の両方がJSONで出力されます。
