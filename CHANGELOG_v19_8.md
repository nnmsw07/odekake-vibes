# v19.8 — Kibun Plans

## 目的
「スポット3件」ではなく「今日の過ごし方3案」を返す体験へ変更。

## 変更
- 新規 `plans.js` を追加し、既存推薦3件を「過ごし方」に組み立てるレイヤーを追加。
- 半日〜1日を単一施設で十分楽しめる場合は、無理に2施設を組み合わせず **ONE PLACE** として提案。
- 短時間スポットは、同じローカルエリアの相性が良い場所と **2ストップ** に構成。
- 「泊まりもあり」では、昼のスポット＋近隣ホテルを **STAY PLAN** として構成可能。
- 結果UIを `TODAY'S PLANS` に変更し、滞在時間・プランの流れを表示。
- プラン詳細ダイアログを追加。各立ち寄り先から従来のスポット詳細へ進める。
- GA4 `plan_open` / `plan_spot_open` を追加。`recommendation_generate` に `plan_count` / `single_plan_count` を追加。
- topbarコピーを `MOOD → DAY` に変更。

## 設計方針
Planは必ず複数スポットではない。滞在価値が十分高い大型施設、美術館、テーマパーク等は単一スポットでも一つの「過ごし方」として扱う。

## 注意
2スポットプランの施設間移動時間はv19.8では厳密なGoogle Routes計算ではなく、同一ローカルエリア＋30分バッファで構成する。次段階でプラン内ルートの実時間検証を追加可能。
