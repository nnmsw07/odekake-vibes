# Apply v20.11.18

Upload `kibun-v20.11.18-hero-rescue.zip` to the repository root, then run the one-line command supplied with the ZIP. The command first extracts the included current `apply_update.sh`, then the script applies the full update, runs regression checks, deploys the Cloudflare Worker, commits, and pushes.
