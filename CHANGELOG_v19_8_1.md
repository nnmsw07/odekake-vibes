# v19.8.1 — Plan duration & copy polish

## 修正
- 「半日くらい」を選んだのに、1〜2時間しか埋まらないONE PLACEが出る問題を修正。
- プラン上部の時間ラベルを「単一スポットの滞在目安」ではなく「ユーザーが選んだ使える時間」に統一。
- 半日以上では、選択時間の約6割を満たさない短時間スポットを単独プランとして出さない。
- 近隣の2スポットでも時間を満たせない場合、別の主役候補へ自動バックフィルする。
- `estimated_minutes` をPlanに保持し、選択時間（plan promise）と各スポットの滞在目安（fact）を分離。

## コピー
カテゴリ名から機械的に作っていた以下のような抽象コピーを廃止。
- 「外の時間を長くする」
- 「手を動かす時間をつくる」

未編集スポットでは、無理に詩的にせず「何をするか」が自然に伝わる文章へ変更。個別の `editorial.title / lead` があるスポットは引き続き編集コピーを優先する。
