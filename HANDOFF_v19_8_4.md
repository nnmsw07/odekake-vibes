# HANDOFF v19.8.4

## 今回の目的

ユーザー実機確認で見つかった「日帰りなのに宿泊コピー」「買い物なのにテーマパーク」という2種類の意図ずれを修正。

## 実装

- `plans.js`
  - `categoryKind()` の Stay 判定から汎用 `resort` を除外。
  - `water_resort` / `resort_pool` は日帰りアクティビティとして扱う。
- `recommender.js`
  - `shoppingIntentEligible()` を追加。
  - shoppingが選択されている場合は、買い物そのものが主役になりやすい施設のみ推薦候補に残す。
- `app.js`
  - 「買い物・複合施設」ブラウズも同じ判定を使用。
- `test_v19_8_4_intent_guard.js`
  - 上記2点の回帰テスト。

## 次に実機で見るところ

1. 日帰り / 1日で龍宮城スパホテル三日月が出たとき、「今日はそのまま泊まる」にならない。
2. 「買い物を楽しみたい」で、グランツリー・スカイツリータウン・ワールドポーターズ等の買い物主体候補が出て、ピューロランド / 八景島が出ない。
3. 複数vibe選択時に shopping intent guard が厳しすぎないか。

Worker / assets 更新なし。
